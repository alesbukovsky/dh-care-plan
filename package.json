{
	"name": "dh-care-plan-workspace",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "bun run --cwd packages/pwa dev",
		"build": "bun run --cwd packages/pwa build",
		"link:cli": "cd packages/core && bun link",
		"test:all": "bun run test:core && bun run test:pwa",
		"test:core": "cd packages/core && bun test --coverage",
		"test:pwa": "bun run --cwd packages/pwa test",
		"verify": "bun run lint && bun run test:all && bun run build",
		"deploy:cf": "bun run verify && bun run --cwd packages/pwa deploy:cf",
		"deploy:cf:preview": "bun run verify && bun run --cwd packages/pwa deploy:cf:preview",
		"lint": "biome lint --error-on-warnings .",
		"lint:fix": "biome check --write --error-on-warnings ."
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.5",
		"@types/bun": "latest",
		"dh-care-plan": "workspace:*"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
