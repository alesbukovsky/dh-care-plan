/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/react" />

/** Injected at build time from the package version. See `define` in vite.config.ts. */
declare const __APP_VERSION__: string;
